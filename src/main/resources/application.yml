spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 123
    url: jdbc:postgresql://localhost:5433/bridal_shower
    platform: postgres
    jpa:
      show-sql: true
      generate-ddl: true
      hibernate:
        ddl-auto: update
      database: postgresql