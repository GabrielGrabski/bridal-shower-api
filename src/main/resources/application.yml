spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    url: ${DB_URL}
    platform: postgres
    jpa:
      show-sql: true
      generate-ddl: true
      hibernate:
        ddl-auto: update
      database: postgresql